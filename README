To edit the parameters involved in AVL and R/B trees (including number of elements and balance factor),
edit the two parameters N and BALANCE_FACTOR in Makefile

To run experiments regarding AVL trees, type
	make avl
	make avl_sorted

To run experiments regarding Red/Black trees, type
	make rb
	make rb_sorted

To run experiments regarding matrix multiplication, type
	make matrix
For the matrix experiments
	run 1: runs each experments using basic matrix multiplication
	run 2: choose the best algrithem for each experment
	run 3: does each experment using each algrithem, and stores the result in a text file name timesForEach